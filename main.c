#include "struct.h"

int main (){


}


GRAPHE lectureFichier(char* fileName){
    FILE f = fopen(fileName,rt);
    fsanf(f,"%lf %lf", &nl, &nbArc);
    fgets(f);
    int i;
    for(i=0; i<nl; i++){

    }
}
